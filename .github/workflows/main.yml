# Exemple d'utilisation des métriques
from metrics import tonal_accuracy, morphological_f1

# Charger les données d'évaluation
predicted_tones = [...]  # Séquences tonales prédites
true_tones = [...]       # Références

# Évaluation tonale
acc = tonal_accuracy(predicted_tones, true_tones)
print(f"Précision tonale: {acc:.2%}")

# Évaluation morphologique
predicted_morph = [...]  # Analyses morphologiques prédites
true_morph = [...]       # Références

morph_scores = morphological_f1(predicted_morph, true_morph)
for cat, scores in morph_scores.items():
    print(f"{cat}: F1={scores['f1']:.2f}, Précision={scores['precision']:.2f}, Rappel={scores['recall']:.2f}")
